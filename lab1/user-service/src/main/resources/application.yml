server:
  port: 0

spring:
  application:
    name: user-service
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2-console
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/test
    hikari:
      username: sa
      password:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
    #        show_sql: true
      format_sql: true
    open-in-view: false

eureka:
  client:
# Service Discorvery? ??(register-with-eureka, fetch-registry ?? ?? ?? ??? ?? ?? true? ??????.).
    register-with-eureka: true
    fetch-registry: true
# eureka end-point(http://127.0.0.1:8761/eureka)? ???? ??? ??
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}

greeting:
  message:  Welcome to the Simple E-commerce

logging:
  level:
    com.example.userservice: DEBUG

token:
  expiration_time: 86400000
  secret: A7CAA960C4AC5D81336AF00778E4F6FE1EDE88F237D01E446B511FCE506E0C310C6BDB5C519DC0EC3D5E45828697EA6EC6828E233EB71E4B79864201370D5DD9